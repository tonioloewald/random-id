{
  "name": "random-id",
  "version": "1.0.0",
  "keywords": ["crypto", "random", "uuid", "randomUUID"],
  "source": "src/index.js",
  "main": "dist/main.js",
  "module": "dist/module.js",
  "scripts": {
    "build": "rm -rf dist && npx standard --fix && bun wiptest && npx parcel build src/index.js"
  },
  "devDependencies": {
    "parcel": "latest",
    "standardjs": "latest"
  },
  "targets": {
    "default": {
      "outputFormat": "esmodule"
    }
  }
}